/**
 * @orion/shared — Public API
 *
 * Everything imported from `@orion/shared` resolves through this barrel.
 * Granular sub-paths (e.g. `@orion/shared/env`) are also available for
 * tree-shaking-sensitive consumers.
 */

export * from "./env.js";
